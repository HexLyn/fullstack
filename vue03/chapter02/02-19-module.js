let base = 100;
const add = (x) => base + x;
const multiply = (x) => base * x;

export { add, multiply };

// export const add = (x) => base + x;
// export const multiply = (x) => base * x;
//이건 최신문법이라 확장자 .mjs로 바꿔줘야 작동.
